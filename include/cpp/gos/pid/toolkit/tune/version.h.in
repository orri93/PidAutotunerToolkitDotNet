/** \defgroup pid_toolkit_lib PID Toolkit
 *
 * \brief The PID Toolkit library
 */

#ifndef GOS_PID_TOOLKIT_TUNE_VERSION_H_
#define GOS_PID_TOOLKIT_TUNE_VERSION_H_

/* The major version, (1, if %PROJECT_VERSION is 1.2.3.4) */
#define GTT_VERSION_MAJOR (@PROJECT_VERSION_MAJOR@)

/* The minor version (2, if %PROJECT_VERSION is 1.2.3.4) */
#define GTT_VERSION_MINOR (@PROJECT_VERSION_MINOR@)

/* The patch version (3, if %PROJECT_VERSION is 1.2.3.4) */
#define GTT_VERSION_PATCH (@PROJECT_VERSION_PATCH@)

/* The build version (4, if %PROJECT_VERSION is 1.2.3.4) */
#define GTT_VERSION_BUILD (@PROJECT_VERSION_TWEAK@)

/* The full version, like 1.2.3.4 */
#define GTT_VERSION         @PROJECT_VERSION@

/* The full version, in string form (suited for string concatenation) */
#define GTT_VERSION_STRING "@PROJECT_VERSION@"

namespace gos {
namespace pid {
namespace toolkit {
namespace tune {
namespace version {

typedef unsigned char Option;

extern const Option WithAll;
extern const Option WithPrefix;
extern const Option WithBuildDateTime;

std::string generate(const Option& option = WithPrefix);

} // namespace version
} // namespace tune
} // namespace toolkit
} // namespace pid
} // namespace gos

#endif
